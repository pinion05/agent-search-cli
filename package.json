{
  "name": "@npmc_5/braive",
  "version": "0.2.0",
  "description": "Agent-focused retrieval CLI that runs Brave search through crawl, semantic reduction, and TOON serialization.",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "braive": "dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "bunx tsc -p tsconfig.build.json",
    "test": "bun test",
    "goldens:live": "bun scripts/capture-agent-browser-like-live-goldens.ts",
    "braive": "bun src/cli.ts",
    "pack:dry-run": "npm pack --dry-run",
    "publish:dry-run": "npm publish --dry-run --access public"
  },
  "dependencies": {
    "agent-browser": "0.9.2",
    "crawlee": "^3.16.0",
    "linkedom": "^0.18.12",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.9.2"
  }
}
